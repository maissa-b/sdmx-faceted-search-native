import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  languageSelector: {
  },
  pickerContainer: {
    position: 'absolute',
    right: 0,
    top:0,
    left:0,
    backgroundColor: 'red',
  },
  modalContainer: {
    display: 'flex',
    flex:1,
    justifyContent: 'center',
    alignItems: 'center',
  }
});
