import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  errorHeader: {
    fontWeight: 'bold',
    fontSize: 20,
    alignItems: 'center',
  },
  errorLabel: {
    fontWeight: 'normal',
    fontSize: 15,
    alignItems: 'center',
  },
});
