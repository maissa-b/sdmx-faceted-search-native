import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  app: {
    display: 'flex',
    flex: 1,
    backgroundColor: '#fff',
  },
  card: {
    display: 'flex',
    flex: 1,
    backgroundColor: '#fff',
  },
  appScrollView: {
    backgroundColor: 'white',
  },
});
